# 微信云托管环境变量配置模板
# 请根据实际情况修改以下配置，并在微信云托管控制台的"环境变量"页面添加

# ===========================================
# 数据库配置 (必填)
# ===========================================
# MySQL数据库连接URL
# 格式：jdbc:mysql://主机地址:端口/数据库名?参数
DB_URL=jdbc:mysql://your-mysql-host:3306/valuesfriends?useSSL=true&serverTimezone=Asia/Shanghai&characterEncoding=utf8

# 数据库用户名
DB_USERNAME=root

# 数据库密码
DB_PASSWORD=@Value1234

# ===========================================
# Redis配置 (必填)
# ===========================================
# Redis主机地址
REDIS_HOST=your-redis-host

# Redis端口
REDIS_PORT=6379

# Redis数据库编号
REDIS_DATABASE=1

# Redis密码（如果没有设置密码，可以留空）
REDIS_PASSWORD=

# ===========================================
# 应用配置 (可选)
# ===========================================
# Spring Boot 激活的配置文件
SPRING_PROFILES_ACTIVE=prod

# JVM参数配置
JAVA_OPTS=-Xms256m -Xmx512m -XX:+UseG1GC -XX:+UseContainerSupport

# ===========================================
# 微信相关配置 (如需要微信功能请填写)
# ===========================================
# 微信公众号配置
WX_MP_APP_ID=your-mp-app-id
WX_MP_SECRET=your-mp-secret
WX_MP_TOKEN=your-mp-token
WX_MP_AES_KEY=your-mp-aes-key

# 微信开放平台配置
WX_OPEN_APP_ID=your-open-app-id
WX_OPEN_APP_SECRET=your-open-app-secret

# ===========================================
# 对象存储配置 (如需要文件上传功能请填写)
# ===========================================
# 腾讯云COS配置
COS_ACCESS_KEY=your-cos-access-key
COS_SECRET_KEY=your-cos-secret-key
COS_REGION=your-cos-region
COS_BUCKET=your-cos-bucket

# ===========================================
# Elasticsearch配置 (如需要搜索功能请填写)
# ===========================================
ES_URIS=http://your-es-host:9200
ES_USERNAME=your-es-username
ES_PASSWORD=your-es-password

# ===========================================
# 接口文档配置 (可选)
# ===========================================
# Knife4j接口文档访问用户名
KNIFE4J_USERNAME=admin
# Knife4j接口文档访问密码
KNIFE4J_PASSWORD=123456

# ===========================================
# 配置说明
# ===========================================
# 1. 请将此文件中的占位符替换为实际值
# 2. 在微信云托管控制台的"环境变量"页面逐一添加这些配置
# 3. 敏感信息（如密码）请妥善保管，不要提交到代码仓库
# 4. 可以根据实际需要删除不使用的配置项